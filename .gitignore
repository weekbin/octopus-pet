# Octopus Pet — gitignore
# 四分类: 组件面/开发面 提交; 中间产物/本地噪音 忽略。
# 可分发产物 (bin/octopus-pet.bin, spritesheet webp, manifest) 提交 — repo 即插件。
# 注意: gitignore 不支持行内注释, 所有 # 必须单独成行。

# ── 中间产物 (可重建, 不提交) ──────────────────────────────
src-tauri/target/
src-tauri/gen/
src-tauri/.tauri/
src-tauri/WixTools/
app/dist/
app/node_modules/
app/.vite/
app/tsconfig.tsbuildinfo
dist/
app/public/assets/octopus/spritesheet-*.miff

# ── 本地构建/审计缓存 ─────────────────────────────────────
.local-audit/
.local-frames/
OCTOPUS_DATA/

# ── Editor / OS ────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# ── Logs ───────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Local config / secrets ─────────────────────────────────
.env
.env.local
*.local
secrets/
secrets.json

# ── Backup / temp ──────────────────────────────────────────
*.bak
*.tmp
.cache/

# ── 无关文档 (不属于 octopus-pet) ─────────────────────────
codebuddy-integration-gap-report.md

# 注: src-tauri/Cargo.lock 必须提交 (应用项目可复现构建, CI cache key 依赖它)
