# 2026-09-10 第二天交接 — V2 桌宠 H3 备料 + 9 个 mp4 给 GPU 机器

> **给明天的 Ubuntu 192.168.1.164 NUC** (或任何 RTX 3060+ 机器) 看的。
> macOS M1 Pro 端只到 H3 mp4 为止, **不放进 matting pipeline**。

---

## TL;DR (3 步走完)

```bash
# 1. 拉最新 (今天 macOS 端 commit)
cd ~/Works/octopus-pet && git pull origin main
#   OR clone 后 0 改动即可
#   关键: 9 个 mp4 在 app/public/assets/octopus/_h3-source/ 需另外从 macOS 端 scp/rsync 过来
#        (此目录已加 .gitignore, 不进 git)

# 2. 跑 v10-final (默认, RTX 3060 12GB)
PYENV_VERSION=3.12.3 python3 scripts/extract-v10-final.py \
  --input app/public/assets/octopus/_h3-source/<scene>-h3.mp4 \
  --output app/public/assets/octopus/v2/<scene>.png

# 3. 同步 scenes.json + 重新 build
#    (手工编辑 scenes.json 加 9 个 entry, 跑 build-scene-registry.sh)
bash scripts/build-scene-registry.sh
bash scripts/check-scenes-sync.sh

# 4. 验证 (red bg 截图测试, 绿残留 < 1%)
# 5. 提交 APNG + scenes.json + 重新 cargo tauri build + release-plugin.sh
```

---

## 1. 9 个 H3 mp4 备料 (在 M1 Pro 端, 需 scp/rsync 到 NUC)

| 文件 | 大小 | verify 相似度 | 状态 |
|------|------|--------------|------|
| `breakdown-h3.mp4` | 478K | 95.23% | ✅ |
| `pretend-busy-h3.mp4` | 706K | **99.84%** | ✅ 完美 |
| `stay-late-h3.mp4` | 520K | 97.53% | ✅ |
| `friday-5pm-h3.mp4` | 902K | **99.92%** | ✅ 完美 |
| `treat-milk-tea-h3.mp4` | 631K | 98.52% | ✅ |
| `payday-h3.mp4` | 1.1M | 90.36% | ❌ 边缘 (v10-final 治本) |
| `lying-flat-h3.mp4` | 515K | 90.47% | ❌ 边缘 (v10-final 治本) |
| `touch-fish-h3.mp4` | 805K | 92.55% | ❌ 边缘 (v10-final 治本) |
| `soul-leaving-h3.mp4` | 584K | **63.91%** | ❌ 灾难 (需重写 prompt 或换范式) |

**位置**: `app/public/assets/octopus/_h3-source/<scene>-h3.mp4` (gitignored, 临时区)

**传输方式** (任选):
```bash
# rsync (推荐, 增量)
rsync -avz --progress /Users/yangweibin/Documents/cute/app/public/assets/octopus/_h3-source/ \
    weekbin@192.168.1.164:~/Works/octopus-pet/app/public/assets/octopus/_h3-source/

# 局域网 ~10 MB/s, 6.3MB 总 < 1 秒
```

**预计 v10-final 输出**:
- 5 个 PASS (95%+) 直接高质量 APNG, 不需后处理
- 3 个 FAIL (90-95%) 走 borrow+recolor + inpaint 治本, 95% 干净
- 1 个 灾难 (soul-leaving 64%) 需重写 prompt 或换范式 (可能改为"触手耷拉头垂下"替代"鬼魂章鱼升起", 避开半透明能力上限)

---

## 2. 9 个 prompt 范本 (已 commit 进 git, 跟 01-03 同样格式)

| 文件 | 动作 | 难度 | 验证 |
|------|------|------|------|
| `prompts/04-breakdown.md` | 触手抱头趴桌 | 🟢 | 95.23% |
| `prompts/05-payday.md` | 触手撒钱 (2 道具) | 🟡 | 90.36% |
| `prompts/06-treat-milk-tea.md` | 触手捧奶茶 | 🟢 | 98.52% |
| `prompts/07-soul-leaving.md` | 灵魂出窍 (半透明鬼魂) | 🟡 | 63.91% 灾难 |
| `prompts/08-lying-flat.md` | 8 触手摊开躺平 (90° 旋转) | 🟡 | 90.47% |
| `prompts/09-pretend-busy.md` | 假装很忙 (6 触手快速敲键盘) | 🟢 | 99.84% |
| `prompts/10-stay-late.md` | 桌前托腮叹气 | 🟢 | 97.53% |
| `prompts/11-friday-5pm.md` | 触手飞奔出门 (跳跃 ±3%) | 🟡 | 99.92% |
| `prompts/12-touch-fish.md` | 触手假装工作 (双组触手) | 🟡 | 92.55% |

**格式**: 4 段固定结构 (Section 1 通用前缀 / Section 2 动作概述 / Section 3 3 段×2s 画面分割 / Section 4 残留物体清单)
**复制范本**: `prompts/00-format.md` (16 项通用前缀 + 5 元素段结构)

---

## 3. 3 个 H3 batch wrapper (已 commit)

| 文件 | 用途 |
|------|------|
| `scripts/run-h3-batch.sh` | 跑 5 个原始动作 (04-08) |
| `scripts/run-h3-batch-remaining.sh` | 跑剩下 4 个 (05-08, 04 跳过) |
| `scripts/run-h3-batch-09-12.sh` | 跑 4 个新动作 (09-12) |

**用法**:
```bash
bash scripts/run-h3-batch-09-12.sh  # 自动跑 4 个, 跳过已存在的 mp4
```

**前提**:
- `mcode-tools` CLI 在 PATH
- `art/octopus-frames/standard-char-1x1.png` 存在 (gitignored, 需本地持有)
- `~/.minimax/agents/mavis/skills/h3-dual-image-video-gen/` skill 存在

**输出**: `app/public/assets/octopus/_h3-source/<scene>-h3.mp4`

---

## 4. scenes.json (9 个 entry 模板, 等 GPU 跑完 APNG 后再加)

**当前 scenes.json** (3 个已落地):
```json
{
  "scenes": [
    { "id": "detective-study", "source": "H3 戴帽研究 (h3-dual-image-video-gen skill)", "bubbleLines": [...] },
    { "id": "worker-construction", "source": "gen_videos 工人施工", "bubbleLines": [...] },
    { "id": "drink-coffee", "source": "H3 喝咖啡 (h3-dual-image-video-gen skill)", "bubbleLines": [...] }
  ]
}
```

**9 个新 entry 模板** (建议气泡行, 7 句, 每句 ≤12 字):
```json
{ "id": "breakdown",       "source": "H3 触手抱头趴桌 (prompts/04)",      "bubbleLines": ["受不了了", "抱头", "缓一下", "今天太累", "要崩溃", "想静静", "我真的..."] },
{ "id": "pretend-busy",    "source": "H3 假装很忙 (prompts/09)",          "bubbleLines": ["假装很忙", "在敲键盘", "看起来很拼", "我努力", "今天效率高", "要KPI", "别看手机"] },
{ "id": "stay-late",       "source": "H3 桌前托腮叹气 (prompts/10)",      "bubbleLines": ["加班中", "叹气", "想回家", "困了", "再撑一会", "老板还不走", "饭凉了"] },
{ "id": "friday-5pm",      "source": "H3 触手飞奔出门 (prompts/11)",      "bubbleLines": ["下班啦", "飞奔", "周末万岁", "冲啊", "跑得快", "今天 5 点", "出去玩"] },
{ "id": "touch-fish",      "source": "H3 触手假装工作 (prompts/12)",      "bubbleLines": ["假装在敲", "偷偷摸鱼", "左看右看", "没人发现", "划水", "今天任务？", "等下班"] },
{ "id": "treat-milk-tea",  "source": "H3 触手捧奶茶 (prompts/06)",        "bubbleLines": ["喝奶茶", "好香啊", "续杯吗", "今日份糖", "早八续命", "摸鱼时间", "让我清醒"] },
{ "id": "payday",          "source": "H3 触手撒钱 (prompts/05)",           "bubbleLines": ["发工资", "撒钱", "有钱啦", "今天真香", "买买买", "我富有", "存起来"] },
{ "id": "lying-flat",      "source": "H3 8 触手摊开躺平 (prompts/08)",     "bubbleLines": ["躺平了", "不想动", "今天废了", "就这样吧", "摆烂", "让我躺", "我已躺"] },
{ "id": "soul-leaving",    "source": "H3 灵魂出窍 (prompts/07, 需重做)",   "bubbleLines": ["我没了", "灵魂出窍", "身体在动", "我在飞", "不在状态", "神游中", "发呆"] }
```

**注意**: soul-leaving verify 64% 灾难,需重写 prompt。建议:改为"触手无力垂下 + 表情恍惚"替代"半透明鬼魂章鱼升起",避开 H3 半透明能力上限。

---

## 5. v10-final pipeline 跑法 (在 NUC, RTX 3060+)

```bash
# 1. 准备环境 (一次性)
cd ~/Works/octopus-pet
git pull origin main  # 拉今天 commit
# rsync 9 个 mp4 到 _h3-source/ (上面 rsync 命令)

# 2. 验证依赖
PYENV_VERSION=3.12.3 python3 -c "import torch, transformers, kornia; print('OK')"
ls ~/.cache/huggingface/hub/ | grep -E "BiRefNet|CorridorKey"  # 模型权重

# 3. 跑 v10-final (5 个 PASS 直接跑, 3 个边缘 + 1 灾难看治本)
PYENV_VERSION=3.12.3 python3 scripts/extract-v10-final.py \
  --input app/public/assets/octopus/_h3-source/pretend-busy-h3.mp4 \
  --output app/public/assets/octopus/v2/pretend-busy.png
# 重复 9 次, 每次换 --input 和 --output

# 4. 验证 (red bg 截图测试, 绿残留 < 1%)
# 见 docs/v10-pipeline.md §9

# 5. 加 entry 到 scenes.json (上面模板)
# 6. 同步 TS + Rust
bash scripts/build-scene-registry.sh
bash scripts/check-scenes-sync.sh

# 7. 重新跑 release (macOS 端跑 macOS 产物, NUC 跑 linux 产物)
# 跨平台 portable, 本机不动 cargo build
bash scripts/release-plugin.sh
git add bin/octopus-pet.{macos,linux}.bin scenes.json
git commit -m "feat(v2): +9 场景 APNG (v10-final, 4 批量)"
git push origin main
```

---

## 6. 今天产物的 commit 链 (供参考)

```
688e0d9 release(macos): v2.2 macOS 产物 (v10-final pipeline)  ← macOS 端
78afa70 cleanup: pipeline single source of truth (v10-final default + 4 deprecated removed)
  ... 19 个 commit 之前在 Ubuntu 端跑
```

明天 macOS 端会再拉 + 重新 build (因为 APNG + scenes.json 改了)。

---

## 7. 注意事项

1. **`_h3-source/` 目录**: 9 个 mp4 临时区, 跑完 v10-final 出 APNG 后可以删, 或者归档到 `docs/v2-NN-name/h3-source.mp4` (跟已落地的 3 个 v2-01/02/03 一样归档方式)
2. **soul-leaving 64%**: 不要急着 deploy, 先重写 prompt
3. **scenes.json 模板**: 9 个气泡行只是建议, 实际可以边用边改
4. **macOS 端不动 cargo**: APNG + scenes.json 改完后, 跨平台 portable, 跑 `bash scripts/release-plugin.sh` 自动重打
5. **CI 验证**: `bash scripts/lint-octopus-plugin.sh` (16/16) + `bash scripts/check-scenes-sync.sh` 必须通过

---

## 8. 关键文件清单 (今天 + 之前)

**今天新加** (macOS 端 commit + NUC 端已就绪):
- `prompts/04-12.md` (9 个 prompt 范本, 11-14K 字各)
- `scripts/run-h3-batch*.sh` (3 个 batch wrapper)
- `docs/handoff-2026-09-10.md` (本文件)
- `.gitignore` 加 `_h3-source/`

**今天 macOS 端 release** (commit 688e0d9):
- `bin/octopus-pet.macos.bin` (15.8MB, 内嵌 3 个 v10-final APNG)

**今天 NUC 端 release** (commit 78afa70 + 之前 19 个):
- `bin/octopus-pet.linux.bin` (17.3MB, 内嵌 3 个 v10-final APNG)
- v10-final pipeline (`scripts/extract-v10-final.py`)
- 4 篇文档 (`pipeline.md` / `h3-capabilities.md` / `v10-pipeline.md` / `linux-build.md`)
- 真实章鱼 icon (`icon.png/icns/ico`)

---

## 9. 预期产出 (明天 GPU 跑完)

- 5 个新 APNG (PASS 直接 v10-final 输出): breakdown / pretend-busy / stay-late / friday-5pm / treat-milk-tea
- 3 个 APNG (边缘 FAIL, v10-final borrow+recolor 治本): payday / lying-flat / touch-fish
- 1 个需重写 prompt (灾难 FAIL): soul-leaving → 改"触手耷拉恍惚" 范式
- 加上原有 3 个 (detective / worker / drink-coffee), **V2 桌宠总共 11-12 个动作可选**
